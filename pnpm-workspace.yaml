allowBuilds:
  # All four are optional native/perf addons. The webpack build and the theme
  # runtime work without them, so their install scripts stay disabled.
  '@parcel/watcher': false
  bufferutil: false
  es5-ext: false
  utf-8-validate: false
